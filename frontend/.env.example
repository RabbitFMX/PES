# Base URL of the PES backend, including the /api path.
# e.g. http://localhost:3001/api in dev.
VITE_API_BASE_URL=

# Supabase project URL and public anon key (Project Settings → API).
# Used by the browser Supabase Auth client for email+password login.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
